/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["vayapublicidad.com"], // Agregar dominio de imágenes externas
    },
  };
  
  module.exports = nextConfig;
  